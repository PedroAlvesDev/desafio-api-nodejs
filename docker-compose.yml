services:
  db:
    image: postgres:latest
    # container_name: postgres_db
    # restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: anakin
      POSTGRES_DB: desafio
    ports:
      - "5433:5432"
    # volumes:
    #   - ./data/postgres:/var/lib/postgresql/data
    # healthcheck:
    #   test: ["CMD-SHELL", "pg_isready -U postgres"]
    #   interval: 10s
    #   timeout: 5s
    #   retries: 5

# volumes:
#   postgres_data:
